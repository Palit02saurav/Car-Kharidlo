<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp"
    android:background="#FFFFFF"
    tools:context=".ui.checkout.Checkout">

    <!-- Car Illustration -->
    <ImageView
        android:id="@+id/img_car"
        android:layout_width="180dp"
        android:layout_height="180dp"
        android:src="@drawable/placeholder_image"
        android:contentDescription="Car"
        android:layout_marginTop="40dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Checkout Title -->
    <TextView
        android:id="@+id/tv_checkout_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Checkout Summary"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="#0D2A4D"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/img_car"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Total Price -->
    <TextView
        android:id="@+id/tv_total_amounts"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Total Amount: ₹ 0"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="#007F00"
        android:layout_marginTop="10dp"
        app:layout_constraintTop_toBottomOf="@id/tv_checkout_title"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Order Message -->
    <TextView
        android:id="@+id/tv_order_messages"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text=""
        android:textSize="16sp"
        android:textStyle="italic"
        android:gravity="center"
        android:textColor="#444444"
        android:layout_marginTop="20dp"
        app:layout_constraintTop_toBottomOf="@id/tv_total_amounts"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Place Order Button -->
    <Button
        android:id="@+id/btn_place_orders"
        android:layout_width="0dp"
        android:layout_height="55dp"
        android:text="Place Order"
        android:textAllCaps="false"
        android:textSize="18sp"
        android:backgroundTint="#0D2A4D"
        android:textColor="#FFFFFF"
        android:layout_marginTop="30dp"
        app:layout_constraintTop_toBottomOf="@id/tv_order_messages"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
